<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="NsComments">
		<select id="CommentsCnt" parameterType="Integer" resultType="Integer">
			select count(*) as cnt from comments where seq_brd = #{seq_brd}
		</select>
		
		<insert id="WriteComments" parameterType="HashMap">
			insert into comments values (seq_comt.nextval, #{seq_brd}, #{mid}, #{comt_content}, sysdate)
		</insert>
		
		<select id = "SelectComments" resultType="Integer">
			select seq_comt.currval as seq_comt from dual
		</select>
		
		<select id = "SelectRealComments" resultType="MyComments" parameterType="Integer">
			select * from comments where seq_comt = #{seq_comt}
		</select>
		

	</mapper>
	
	