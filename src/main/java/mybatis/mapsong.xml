<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="NsSong">
		<select id="SelectSongAndDescripList" resultType="MySong" parameterType="Integer">
			select * from song s full outer join song_description d on s.seq_song = d.seq_song where s.seq_song = #{seq_song}
		</select>
	
	</mapper>
	
